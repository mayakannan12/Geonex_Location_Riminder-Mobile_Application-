<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer glow ring -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/ring_border" />
            <size android:width="96dp" android:height="96dp" />
        </shape>
    </item>

    <!-- Inner fill -->
    <item
        android:bottom="6dp"
        android:left="6dp"
        android:right="6dp"
        android:top="6dp">
        <shape android:shape="oval">
            <solid android:color="@color/ring_fill" />
            <stroke
                android:width="1.5dp"
                android:color="@color/ring_inner" />
            <size android:width="84dp" android:height="84dp" />
        </shape>
    </item>
</layer-list>